// Opposite numbers
// Very simple, given a number, find its opposite.

function opposite(number) {
  if (number[0] != '-') {
    return -number
  }
  else {
    return number.trim[0]
  }
}

// short version
function opposite(number) {
  return -number
}
